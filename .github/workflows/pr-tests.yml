name: Pull Request Tests
on:
  pull_request:
    branches: [main]
jobs:
  run-tests:
    uses: ./.github/workflows/reusable-test-template.yml
    with:
      node-version: "18"
      test-tag: regression
